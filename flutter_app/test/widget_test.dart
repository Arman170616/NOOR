import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder — full integration tests require a running backend and Google auth.
    expect(true, isTrue);
  });
}
